const axios = require('axios');

/**
 * ===========
 * Exercise 01
 * ===========
 * 
 * Write an async await function to fetch data from an URL
 * and format the response to the expected string which is
 * {Name} is from {City} working for {Company}
 * Use the function name fetchDataAndGenerateResult to
 * control two other functions, fetchDataPromisified and formatResultString.
 * fetchDataPromisified is responsible for using axios to fetch the data and 
 * formatResultString is responsible for formatting the data as expected.
 * Use the try catch mehtod in the async await function
 */
const fetchDataPromisified = (/* insert params here */) => {
  /* insert code here */
};

const formatResultString = (/* insert params here */) => {
  /* insert code here */
};

const fetchDataAndGenerateResult = async (/* insert params here */) => {
  /* insert code here */
}

module.exports = {
  fetchDataAndGenerateResult,
}