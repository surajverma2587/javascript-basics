const axios = require('axios');

/**
 * ===========
 * Exercise 01
 * ===========
 * From the promise-async-await example convert all the functions
 * fetchDataPromisified and formatResultString to async await and
 * remove all implementations of promises
 */
const fetchDataAsync = async (/* insert params here */) => {
  /* insert code here */
};

const formatResultStringAsync = async (/* insert params here */) => {
  /* insert code here */
};

const fetchDataAndGenerateResultAsync = async (/* insert params here */) => {
  /* insert code here */
}

module.exports = {
  fetchDataAndGenerateResultAsync,
}